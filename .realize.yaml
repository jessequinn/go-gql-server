settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: go-gql-server
  path: .
  args:
  - run
  commands: {}
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
